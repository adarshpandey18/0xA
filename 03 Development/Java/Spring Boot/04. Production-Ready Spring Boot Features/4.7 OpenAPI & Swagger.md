# OpenAPI & Swagger in Spring Boot

## 1. Springdoc OpenAPI

Springdoc OpenAPI simplifies integration of **Swagger UI** and **OpenAPI 3** into Spring Boot apps. It automatically generates API documentation from your **Spring MVC / WebFlux endpoints**.

### Add Dependency (Maven)

```xml
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>2.5.0</version> <!-- check for latest -->
</dependency>
```

---

## 2. Access API Docs

- OpenAPI Spec (JSON): `http://localhost:8080/v3/api-docs`
    
- Swagger UI: `http://localhost:8080/swagger-ui/index.html`
    

---

## 3. Basic Configuration

In `application.properties`:

```properties
springdoc.api-docs.title=My API Documentation
springdoc.api-docs.description=Sample API Documentation
springdoc.api-docs.version=1.0.0
springdoc.api-docs.path=/my-api-docs
springdoc.swagger-ui.path=/my-swagger-ui
```

This customizes the **path & metadata**.

---

## 4. Create a REST Controller Example

```java
@RestController
@RequestMapping("/api/books")
public class BookController {

    @GetMapping("/{id}")
    public Book getBook(@PathVariable Long id) {
        return new Book(id, "Clean Code", "Robert C. Martin");
    }

    @PostMapping
    public Book addBook(@RequestBody Book book) {
        return book; // pretend to save
    }
}
```

Model:

```java
public class Book {
    private Long id;
    private String title;
    private String author;

    // constructor, getters, setters
}
```

Run with:

```bash
mvn spring-boot:run
```

Then visit:  
👉 [http://localhost:8080/swagger-ui/index.html](http://localhost:8080/swagger-ui/index.html)

---

## 5. Customize API Metadata

You can annotate your **main class**:

```java
@OpenAPIDefinition(
    info = @Info(
        title = "Book API",
        version = "1.0",
        description = "API for managing books",
        contact = @Contact(name = "Your Name", email = "you@email.com")
    )
)
@SpringBootApplication
public class BookApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(BookApiApplication.class, args);
    }
}
```

---

## 6. Document Endpoints with Annotations

Enhance Swagger docs with `@Operation`, `@ApiResponse`:

```java
@Operation(summary = "Get book by ID", description = "Provide an ID to lookup a book")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "Found the book"),
    @ApiResponse(responseCode = "404", description = "Book not found")
})
@GetMapping("/{id}")
public Book getBook(@PathVariable Long id) {
    return new Book(id, "Clean Code", "Robert C. Martin");
}
```

---

#### With this setup:

- **Developers** can explore and test APIs live.
    
- **Clients** can generate client SDKs directly from `/v3/api-docs`.
    

---
